Plus longue distance minimale entre deux nœuds

# $$D=\max_{i,j\in\mathcal{V}}\mathrm{dist}(i,j)$$
avec $\mathcal{V}$ l'ensemble des nœuds et $\mathrm{dist}(i,j)$ la longueur du plus court chemin reliant $i$ à $j$ 